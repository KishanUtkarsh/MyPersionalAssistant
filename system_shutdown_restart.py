import os
import sys
class system_shutdown_restart:
    def __init__(self, shutdown,restart,abort_shutdown):
        shutdown.self  = shutdown
        restart.self  = restart
        abort_shutdown.self = abort_shutdown

    def shutdown(True):


